<!-- clientes.ejs -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Clientes</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <!-- En lugar de enlazar a CDN, puedes enlazar a tu archivo CSS generado si lo prefieres -->
    <!-- <link href="/styles.css" rel="stylesheet"> -->
</head>
<body class="bg-gray-900 text-white flex justify-center items-center h-screen">
    <div class="container mx-auto px-20">
        <h1 class="text-3xl font-bold mb-8 text-center">Lista de Clientes</h1>
        <table class="w-full sm:w-2/3 md:w-1/2 lg:w-1/3 xl:w-1/4 min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-800">
                <tr>
                    <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-300 uppercase tracking-wider text-center">Nombre</th>
                    <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-300 uppercase tracking-wider text-center">Acciones</th>
                </tr>
            </thead>
            <tbody class="bg-gray-700 divide-y divide-gray-200">
                <% clientes.forEach(cliente => { %>
                    <tr>
                        <td class="px-6 py-4 whitespace-nowrap text-center"><%= cliente.nombre %></td>
                        <td class="px-6 py-4 whitespace-nowrap text-center">
                            <a href="/clientes/<%= cliente.id %>/edit">
                                <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
                                    Editar
                                </button>
                            </a>
                            <form action="/clientes/<%= cliente.id %>?_method=DELETE" method="POST" style="display: inline;">
                                <button type="submit" class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded">
                                    Eliminar
                                </button>
                            </form>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
</body>
</html>
